SPEC COMPLETION MAP — endpoints currently emitting `looseObjectSchema`
Generated: 2026-05-17
Source files: packages/api-routes/src/openapi.ts (spec) + packages/api-routes/src/*.ts (handlers)

================================================================================
## Tier 1 — schema already exists in contracts, just register + flip the spec call
================================================================================

/apply
  contracts schema: projectDtoSchema (ProjectDto, already registered)
  handler: packages/api-routes/src/apply.ts:304-323 returns the full ProjectDto shape
  spec: packages/api-routes/src/openapi.ts:825 (rawJsonResponse)
  est: 5 min
  notes: Handler returns exactly the ProjectDto fields. Just flip to jsonResponse('Config applied.', 'ProjectDto').

/projects/:name/bing/inspections
  contracts schema: bingUrlInspectionDtoSchema (exists, NOT registered)
  handler: packages/api-routes/src/bing.ts:382-415 (returns array)
  spec: packages/api-routes/src/openapi.ts:1783
  est: 5 min
  notes: Register `BingUrlInspectionDto` in openapi-schemas.ts, swap to jsonArrayResponse.

/projects/:name/bing/inspect-url
  contracts schema: bingUrlInspectionDtoSchema (same)
  handler: packages/api-routes/src/bing.ts:418-544 (single object)
  spec: packages/api-routes/src/openapi.ts:1810
  est: 5 min
  notes: Same registration covers both. Use jsonResponse('...', 'BingUrlInspectionDto').

/projects/:name/bing/performance
  contracts schema: bingKeywordStatsDtoSchema (exists, NOT registered)
  handler: packages/api-routes/src/bing.ts:689-716 (returns array)
  spec: packages/api-routes/src/openapi.ts:1875
  est: 5 min
  notes: Register `BingKeywordStatsDto`, use jsonArrayResponse.

/projects/:name/bing/request-indexing (also listed in larger map but cheap)
  contracts schema: bingSubmitResultDtoSchema (exists, NOT registered)
  handler: packages/api-routes/src/bing.ts:583-687 (returns {summary, results: BingSubmitResultDto[]})
  spec: packages/api-routes/src/openapi.ts:1862
  est: 10 min
  notes: Wrap in a tiny `BingIndexingRequestResponse` schema (summary + results array). Not in your target list but trivial while in bing.ts.

/projects/:name/google/indexing/request
  contracts schema: indexingRequestResultDtoSchema (exists, NOT registered)
  handler: packages/api-routes/src/google.ts:986-1094 (returns {summary, results})
  spec: packages/api-routes/src/openapi.ts:1659
  est: 10 min
  notes: Need a tiny IndexingRequestResponse wrapper around the existing element schema, then register both.

/projects/:name/google/gsc/deindexed
  contracts schema: gscDeindexedRowSchema (exists, NOT exported as type)
  handler: packages/api-routes/src/google.ts:607-653 (returns array)
  spec: packages/api-routes/src/openapi.ts:1561
  est: 10 min
  notes: Export `GscDeindexedRowDto` type from contracts/src/google.ts (1-liner), register, use jsonArrayResponse.

/projects/:name/report
  contracts schema: ProjectReportDto interface exists at packages/contracts/src/report.ts:659 BUT it is a plain TS interface, NOT a Zod schema
  handler: packages/api-routes/src/report.ts:2087-2090
  spec: packages/api-routes/src/openapi.ts:2516
  est: 30+ min (move to Tier 3)
  notes: ProjectReportDto is the largest DTO in the codebase (13 sub-sections, hundreds of fields across executiveSummary, citationScorecard, competitorLandscape, mentionLandscape, aiSourceOrigin, gsc, ga, socialReferrals, aiReferrals, serverActivity, indexingHealth, citationsTrend, whatsChanged, insights, recommendedNextSteps, actionPlan, clientSummary). Writing the Zod schema is doable but large — SEE TIER 3.

================================================================================
## Tier 2 — schema needs writing, small surface (≤ ~10 fields)
================================================================================

/settings
  fields: ~3 top-level keys (providers[], google.configured, bing.configured) where providers[] is ~8 fields (name, displayName?, keyUrl?, modelHint?, model?, configured, quota?, vertexConfigured?)
  handler: packages/api-routes/src/settings.ts:49-53
  spec: packages/api-routes/src/openapi.ts:944
  est: 15 min

/cdp/status
  fields: ~5 (connected, endpoint, version?, browserVersion?, targets[]) where targets[] is 3 fields (name, alive, lastUsed)
  handler: packages/api-routes/src/cdp.ts:95-102 (delegates to opts.getCdpStatus)
  spec: packages/api-routes/src/openapi.ts:1263
  est: 15 min

/projects/:name/bing/status
  fields: 5 (connected, domain, siteUrl, createdAt, updatedAt)
  handler: packages/api-routes/src/bing.ts:194-207
  spec: packages/api-routes/src/openapi.ts:1708 — ALREADY mapped to 'BingConnectionDto'. Spec bug: handler returns a different shape than bingConnectionDtoSchema (no `id`; has `connected`). NEEDS NEW `BingStatusDto` schema OR amend `bingConnectionDtoSchema`.
  est: 15 min

/projects/:name/bing/connect
  fields: 4 returned (connected, domain, siteUrl, availableSites[]) where availableSites is {url, verified}
  handler: packages/api-routes/src/bing.ts:122-170
  spec: packages/api-routes/src/openapi.ts:1685 — ALREADY mapped to 'BingConnectionDto' (spec bug: connect returns availableSites + connected — different shape)
  est: 15 min

/projects/:name/bing/sites
  fields: 1 wrapper ({sites: ApiBingSite[]}) where ApiBingSite = {url, verified}
  handler: packages/api-routes/src/bing.ts:210-218
  spec: packages/api-routes/src/openapi.ts:1720
  est: 10 min
  notes: New `BingSiteDto` (2 fields) + `BingSitesResponse` wrapper.

/projects/:name/ga/status
  fields: ~7 (connected, propertyId, clientEmail, authMethod, lastSyncedAt, createdAt, updatedAt)
  handler: packages/api-routes/src/ga.ts:382-410
  spec: packages/api-routes/src/openapi.ts:2340
  est: 15 min
  notes: Handler returns `authMethod` ('service-account' | 'oauth') — web's ApiGaStatus is MISSING `authMethod`. Spec bug.

/projects/:name/ga/sync
  fields: 7 (synced, rowCount, aiReferralCount, socialReferralCount, days, syncedAt, syncedComponents?)
  handler: packages/api-routes/src/ga.ts:644-652
  spec: packages/api-routes/src/openapi.ts:2365
  est: 15 min
  notes: Handler returns `syncedComponents` conditionally — web's ApiGaSyncResult is MISSING `syncedComponents`. Spec bug.

/projects/:name/google/properties
  fields: 1 wrapper ({sites: ApiGoogleProperty[]}) where ApiGoogleProperty = {siteUrl, permissionLevel}
  handler: packages/api-routes/src/google.ts:358-370
  spec: packages/api-routes/src/openapi.ts:1398
  est: 10 min

/projects/:name/google/gsc/sitemaps
  fields: 1 wrapper ({sitemaps: ApiGscSitemap[]}) where ApiGscSitemap = ~9 fields incl. nested contents[]
  handler: packages/api-routes/src/google.ts:825-841 (delegates to listSitemaps)
  spec: packages/api-routes/src/openapi.ts:1595
  est: 15 min
  notes: Capture nested contents[] = {type, submitted, indexed}. Use real Google response fixture (per `feedback_external_api_test_fixtures` memory — don't invent field names).

================================================================================
## Tier 3 — schema needs writing, large surface (>10 fields or nested)
================================================================================

/projects/:name/timeline
  fields: array of {query, runs[], providerRuns?, modelRuns?} where each run = ~10 fields (runId, createdAt, citationState, transition, answerMentioned?, visibilityState?(legacy), visibilityTransition?(legacy), mentionState?, mentionTransition?, location?)
  handler: packages/api-routes/src/history.ts:112-270
  spec: packages/api-routes/src/openapi.ts:871
  est: 30 min
  notes: 4 top-level + nested providerRuns / modelRuns as Record<string, runEntry[]>. Reuse a single TimelineRunEntryDto.

/projects/:name/bing/coverage
  fields: ~4 top-level (summary{5}, lastInspectedAt, indexed[], notIndexed[], unknown[]) where each entry = 9 fields
  handler: packages/api-routes/src/bing.ts:244-352
  spec: packages/api-routes/src/openapi.ts:1758 — ALREADY mapped to 'BingCoverageSnapshotDto' which is WRONG (that schema is the daily history snapshot, 4 fields). SPEC BUG. Should use `bingCoverageSummaryDtoSchema` (already in contracts + registered as BingCoverageSnapshotDto). The summary schema already covers this — just swap the ref.
  est: 5 min (actually Tier 1 once we accept the spec-name fix). The contracts schema bingCoverageSummaryDtoSchema EXISTS but is NOT in the schema table; `bingCoverageSnapshotDtoSchema` is registered under both `BingCoverageSnapshotDto` AND used for /coverage incorrectly.
  → MOVE TO TIER 1 with `BingCoverageSummaryDto` registration.

/projects/:name/ga/traffic
  fields: ~30+ top-level (totalSessions, totalOrganic/Direct/Users, topPages[], aiReferrals[], aiReferralLandingPages[], aiSessionsDeduped, aiUsersDeduped, aiSessionsBySession, aiUsersBySession, socialReferrals[], socialSessions, socialUsers, channelBreakdown{5 buckets x 3 fields}, organicSharePct, aiSharePct, aiSharePctBySession, directSharePct, socialSharePct, otherSessions, otherSharePct, otherSharePctDisplay, organicSharePctDisplay, aiSharePctDisplay, aiSharePctBySessionDisplay, directSharePctDisplay, socialSharePctDisplay, lastSyncedAt, periodStart, periodEnd)
  handler: packages/api-routes/src/ga.ts:664-984 (large)
  spec: packages/api-routes/src/openapi.ts:2378
  est: 60+ min
  notes: Largest single endpoint in this batch. ga4ChannelBreakdownDtoSchema and ga4ChannelBucketDtoSchema and ga4AiReferralDtoSchema and ga4AiReferralLandingPageDtoSchema and ga4SocialReferralDtoSchema already exist (contracts/src/ga.ts). The trafficSummary fragment (ga4TrafficSummaryDtoSchema) is the closest existing schema — see line 83. The HANDLER returns a near-superset of ga4TrafficSummaryDtoSchema. Consider extending that existing schema (rename or alias) instead of writing a fresh one.

/projects/:name/google/gsc/inspections
/projects/:name/google/gsc/inspect
  fields: 12 (id, url, indexingState, verdict, coverageState, pageFetchState, robotsTxtState, crawlTime, lastCrawlResult, isMobileFriendly, richResults[], referringUrls[], inspectedAt)
  handler: packages/api-routes/src/google.ts:503-568 (inspect) and 574-604 (inspections list)
  spec: packages/api-routes/src/openapi.ts:1537, 1549 — ALREADY mapped to 'GscUrlInspectionDto'. BUT `gscUrlInspectionDtoSchema` in contracts/src/google.ts:50-63 is MISSING the `referringUrls` field that both the handler returns and the web's ApiGscInspection declares. SPEC BUG: extend `gscUrlInspectionDtoSchema` to add `referringUrls: z.array(z.string()).default([])`.
  est: 5 min (Tier 1 — bugfix on existing schema, not a new schema)
  → MOVE TO SPEC-BUGS-AND-TIER-1.

/projects/:name/report
  est: 60+ min
  notes: ProjectReportDto exists as a TS interface but NOT a Zod schema. Writing a Zod schema for it requires schematizing all 13 sub-sections from packages/contracts/src/report.ts. Large effort. May want to defer or write incrementally (start with `meta` + `executiveSummary` + 1 nested section, leave others as `z.unknown()` placeholders, harden iteratively).

================================================================================
## Spec bugs to fix while we're in there
================================================================================

/projects/:name/bing/coverage
  Issue: mapped to `BingCoverageSnapshotDto` (4-field daily history shape) but actually returns `BingCoverageSummaryDto` shape (summary + indexed/notIndexed/unknown arrays).
  Fix: register `bingCoverageSummaryDtoSchema` as `BingCoverageSummaryDto`; swap the ref at openapi.ts:1758.

/projects/:name/bing/status
  Issue: mapped to `BingConnectionDto` but handler returns `{connected, domain, siteUrl, createdAt, updatedAt}` — no `id`, has `connected` (not in DTO). Two different shapes share the name.
  Fix: introduce a `BingStatusDto` schema (5 fields) and use it. Don't conflate connection record (`id`-bearing) with status response.

/projects/:name/bing/connect
  Issue: mapped to `BingConnectionDto` but handler returns `{connected, domain, siteUrl, availableSites[]}` — a different shape again. Three endpoints, three real shapes, one DTO name in spec.
  Fix: new `BingConnectResponseDto` with the actual shape.

/projects/:name/bing/set-site
  Issue: mapped to `BingConnectionDto` but handler returns `{siteUrl}` only.
  Fix: tiny `BingSetSiteResponseDto` ({siteUrl: string}) or rawJsonResponse for the trivial case.

/projects/:name/google/gsc/performance
  Issue: spec uses `jsonResponse('...', 'GscSearchDataDto')` but handler returns an ARRAY of GscSearchDataDto rows (web's ApiGscPerformanceRow[] confirms). Should be `jsonArrayResponse`. SDK consumers currently see a single object instead of an array.
  Fix: change to jsonArrayResponse at openapi.ts:1496.

/projects/:name/google/gsc/inspect + /gsc/inspections
  Issue: `gscUrlInspectionDtoSchema` is missing `referringUrls: string[]`. Handler returns it; web type declares it; schema drops it during codegen so the generated client loses the field.
  Fix: append `referringUrls: z.array(z.string()).default([])` to `gscUrlInspectionDtoSchema` in packages/contracts/src/google.ts:50-63.

/projects/:name/ga/status
  Issue: handler returns `authMethod: 'service-account' | 'oauth'` but web's ApiGaStatus is missing it.
  Fix: include `authMethod` when writing the new GaStatusResponseDto.

/projects/:name/ga/sync
  Issue: handler conditionally returns `syncedComponents` but web's ApiGaSyncResult omits it.
  Fix: optional `syncedComponents` field in the new schema.

/projects/:name/google/gsc/sitemaps (and /bing/sites etc.)
  Issue: external API responses — use REAL fixtures (per memory: `feedback_external_api_test_fixtures`) rather than inventing field shapes. Capture a real `listSitemaps()` response before writing the schema.

================================================================================
## Ranking — what to do FIRST (next 30 min of work)
================================================================================

Recommendation:

1. START with the 4 already-existing-schema Tier-1 wins in `bing.ts` (inspections, inspect-url, performance, request-indexing). These are 5–10 min each, no contract changes — just register existing schemas in `openapi-schemas.ts` and flip 4 `rawJsonResponse` calls to `jsonResponse`/`jsonArrayResponse`. This unblocks the BingSection web aliasing in one tight PR with the lowest blast radius.

2. THEN do the `/apply` swap (single 5-min flip to `ProjectDto`, no schema changes — the handler already returns exactly that shape). This is the highest-value single-line change in the entire batch because every config-as-code consumer benefits.

3. THEN fix the in-place spec bugs that don't require new schemas: extend `gscUrlInspectionDtoSchema` with `referringUrls` (one-line addition unblocks 2 endpoints) and flip `gsc/performance` to `jsonArrayResponse` (one-line correction). These are spec-truth fixes that consumers are already paying for as silent generated-client drift. Save the GA traffic / Report DTO work for a follow-up because their surface area is 10x larger than everything else combined.
